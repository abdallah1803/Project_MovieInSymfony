{% block title %}Film{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('assets/css/login.css') }}">
{% endblock %}

{% block body %}
    <div class="login-box">
        {{ form_start(form) }}
            {{ form(form)}}
        {{ form_end(form) }}
        <a href="{{ path('app_user_register') }}">Create an account</a>
    </div>
{% endblock %}


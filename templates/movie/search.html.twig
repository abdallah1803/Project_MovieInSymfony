{% extends 'base.html.twig' %}

{% block body %}
    <br><br>
    <center>
    {{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.search) }}
    {{ form_end(form) }}

    {% if movies %}
        <h1>Résultats de la recherche pour "{{ app.request.get('title') }}"</h1>

        <ul>
            {% for movie in movies %}
                <li>{{ movie.title }}</li>
            {% endfor %}
        </ul>
    {% endif %}
    </center>
{% endblock %}

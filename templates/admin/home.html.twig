{% extends 'base.html.twig' %}


{% block body %}

    <header>
        <div class="company-logo">DA</div>
        <nav class="navbar">
            <ul class="nav-items">
                <li class="nav-item"><a href="{{ path('movie_home') }}" class="nav-link">HOME</a></li>

                <li class="nav-item"><a href="{{ path('admin_home') }}" class="nav-link">ADMIN</a></li>
                {% if app.user %}
                    <li class="nav-item"><a href="{{ path('app_user_logout') }}" title="Logout" class="nav-link">{{ app.user.username }}</a></li>
                {% else %}
                    <li class="nav-item"><a href="{{ path('app_user_login') }}" class="nav-link">LOGIN</a></li>
                {% endif %}

            </ul>
        </nav>
        <div class="menu-toggle">
            <i class="bx bx-menu"></i>
            <i class="bx bx-x"></i>
        </div>
    </header>



    <center>


        <br><br>
        <a href="{{ path('admin_user_list') }}"><img src="{{ asset('images/user.png') }}" style="width: 80px" ></a>
        <br><br>
        <a href="{{ path('add_movie') }}"><img src="{{ asset('images/upload.png') }}" style="width: 80px"></a>
        <br><br><br>
    </center>

{% endblock %}
